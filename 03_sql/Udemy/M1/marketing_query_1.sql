-- A equipe de markegint precisa fazer um pesquisa sobre nomes mais comuns de seus clientes, e precisa de nome e sobrenome de todos os clientes que estão cadastrados no sistema. 

-- Os nomes dos clientes devem estar na tabela person.Person

SELECT * FROM person.Person;

-- Primeiro e último nome: FirstName, LastName

SELECT FirstName, LastName FROM person.Person;