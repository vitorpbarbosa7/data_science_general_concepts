#ajuda dentro do R


mean("8")

mean(8)

meaan(5,8,9)

help.start()

help.start() #ajuda geral do R

help("*")

help(mean)

#Essa função é retornada na forma de vetor
help.search("mean")[4]

#Maneira legal de utilizar o help
?sd #mesmo que help
??sd #mesmo que help.search

?mean

apropos("kill")
apropos("plot")
apropos("deviation")
apropos("apropos")
help("apropos")
apropos("DOE")
apropos("experiment")
apropos("design")
example("plot.design")
example("plot")

apropos("describe")

#Todas funções que contêm o nome help
apropos("help") #funÃ§Ãµes contendo "help"
#Funções que contêm o nome mean
apropos("mean")

#exemplos de uso
example(mean)

example(plot)

